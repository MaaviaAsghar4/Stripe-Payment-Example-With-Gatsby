import React from "react";
import { Link } from "gatsby";
const Success = () => {
  return (
    <div>
      Successfully Performed the Transactions
      <Link to="/">Go to Home</Link>
    </div>
  );
};

export default Success;
